# Unverified source snapshot

This directory is not an approved application distribution. It contains a large source snapshot branded as another product, but no repository license, upstream reference, commit identity, or ownership record was found. Do not publish, redistribute, deploy, or use it with real credentials or data until provenance and licensing are resolved.

The normal `start` command is deliberately gated. See `PROVENANCE_REQUIRED.md` before any isolated audit.
